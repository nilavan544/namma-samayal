{
  "buildCommand": "cd Recipe-Finder && npm install && npm run build",
  "outputDirectory": "Recipe-Finder/dist",
  "installCommand": "cd Recipe-Finder && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
